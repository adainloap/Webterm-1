<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('includes/head.ejs', { title: 'WebTerm - 2FA' }) %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen">
  <div class="w-full max-w-md p-8 space-y-8 bg-gray-800 rounded-lg shadow-lg">
    <h1 class="text-3xl font-bold text-center">Two-Factor Authentication</h1>

    <div id="message-container" class="text-center font-medium">
      <% if (error) { %>
        <p class="text-red-500"><%= error %></p>
      <% } %>
    </div>
    
    <div class="space-y-4">
      <% if (twoFactorMethod === 'authenticator') { %>
        <div id="auth-app-section" class="p-6 border border-gray-700 rounded-lg space-y-4">
          <h2 class="text-xl font-semibold">Authenticator App</h2>
          <p class="text-gray-400">
            Enter the code from your authenticator app.
          </p>
          <form action="/2fa-login" method="POST" class="space-y-4">
            <input type="text" name="token" placeholder="Enter 6-digit code" required class="w-full px-4 py-2 text-gray-900 bg-gray-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            <button type="submit" class="w-full px-4 py-2 font-bold text-white bg-blue-600 rounded-md hover:bg-blue-700 transition-colors">
              Verify Code
            </button>
          </form>
        </div>
      <% } else if (twoFactorMethod === 'email') { %>
        <div id="email-2fa-section" class="p-6 border border-gray-700 rounded-lg space-y-4">
          <h2 class="text-xl font-semibold">Email</h2>
          <p class="text-gray-400">
            A one-time code has been sent to your email.
          </p>
          <form id="email-2fa-form" action="/2fa-verify-email" method="POST" class="space-y-4">
            <input type="text" name="emailToken" placeholder="Enter code from email" required class="w-full px-4 py-2 text-gray-900 bg-gray-200 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            <button type="submit" class="w-full px-4 py-2 font-bold text-white bg-green-600 rounded-md hover:bg-green-700 transition-colors">
              Verify Code
            </button>
          </form>
          <button id="resend-email-code" class="w-full text-sm text-gray-400 mt-2 hover:underline">Resend code</button>
        </div>
      <% } else if (twoFactorMethod === 'webauthn') { %>
        <div id="webauthn-login-section" class="p-6 border border-gray-700 rounded-lg space-y-4">
          <h2 class="text-xl font-semibold">Fingerprint / FaceID</h2>
          <button id="webauthn-start-login" class="w-full px-4 py-2 font-bold text-white bg-purple-600 rounded-md hover:bg-purple-700 transition-colors">
            Login with Fingerprint / FaceID
          </button>
        </div>
      <% } %>
    </div>
  </div>

  <script src="/js/webauthn.js"></script>
</body>
</html>